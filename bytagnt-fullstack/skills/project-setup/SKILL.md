---
name: project-setup
description: One-time project initialization skill. Sets up design system (theme, tokens, typography). Run ONCE at project start before implementing features.
---

# Project Setup Skill

Initialize the design system. This skill should be run **ONCE** at project start to set up:
- Color theme selection
- Design token generation
- Typography system
- SCSS structure

## Usage

```
/project-setup
```

## Workflow

### Step 1: Theme Selection

Present the available themes from `/theme-factory`:

```
Available Themes:

1. Ocean Depths     - Professional maritime (Blue/Teal)
2. Sunset Boulevard - Warm vibrant (Orange/Pink)
3. Forest Canopy    - Natural earth tones (Green/Brown)
4. Modern Minimalist - Clean grayscale (Gray/Black)
5. Golden Hour      - Rich autumnal (Gold/Orange)
6. Arctic Frost     - Cool crisp winter (Light Blue/White)
7. Desert Rose      - Soft sophisticated (Pink/Beige)
8. Tech Innovation  - Bold modern tech (Blue/Purple) [RECOMMENDED]
9. Botanical Garden - Fresh organic (Green/Cream)
10. Midnight Galaxy - Dramatic cosmic (Dark Purple/Blue)

Which theme would you like for your project?
```

### Step 2: Read Selected Theme

After user selects a theme, read the theme file:

```bash
cat .claude/skills/theme-factory/themes/[selected-theme].md
```

Extract:
- Primary color
- Secondary color
- Font family
- Font weights

### Step 3: Generate Design Tokens

Use the design token generator:

```bash
python .claude/skills/ui-design-system/scripts/design_token_generator.py \
  "#1976d2" \  # Primary color from theme
  modern \     # Style
  scss         # Format
```

### Step 4: Create SCSS Files

Create the following files in `frontend/src/styles/`:

#### _colors.scss
```scss
// Color System
// Generated by /project-setup
// Theme: [Selected Theme]

// Primary Palette
$primary: #1976d2;
$primary-light: #63a4ff;
$primary-dark: #004ba0;
$primary-contrast: #ffffff;

// Secondary/Accent
$accent: #ff4081;
$accent-light: #ff79b0;
$accent-dark: #c60055;
$accent-contrast: #ffffff;

// Semantic Colors
$success: #4caf50;
$warning: #ff9800;
$error: #f44336;
$info: #2196f3;

// Neutral Palette
$gray-50: #fafafa;
$gray-100: #f5f5f5;
$gray-200: #eeeeee;
$gray-300: #e0e0e0;
$gray-400: #bdbdbd;
$gray-500: #9e9e9e;
$gray-600: #757575;
$gray-700: #616161;
$gray-800: #424242;
$gray-900: #212121;

// Surface Colors
$background: #fafafa;
$surface: #ffffff;
$surface-variant: #f5f5f5;

// Text Colors
$text-primary: rgba(0, 0, 0, 0.87);
$text-secondary: rgba(0, 0, 0, 0.6);
$text-disabled: rgba(0, 0, 0, 0.38);
$text-on-primary: #ffffff;

// Border Colors
$divider: rgba(0, 0, 0, 0.12);
$border: rgba(0, 0, 0, 0.23);
```

#### _typography.scss
```scss
// Typography System
// Generated by /project-setup

// Font Families
$font-family-base: 'Roboto', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
$font-family-mono: 'Roboto Mono', 'Consolas', 'Monaco', monospace;

// Font Sizes (Modular Scale: 1.25)
$font-size-xs: 0.75rem;    // 12px
$font-size-sm: 0.875rem;   // 14px
$font-size-base: 1rem;     // 16px
$font-size-lg: 1.125rem;   // 18px
$font-size-xl: 1.25rem;    // 20px
$font-size-2xl: 1.5rem;    // 24px
$font-size-3xl: 1.875rem;  // 30px
$font-size-4xl: 2.25rem;   // 36px
$font-size-5xl: 3rem;      // 48px

// Font Weights
$font-weight-light: 300;
$font-weight-regular: 400;
$font-weight-medium: 500;
$font-weight-bold: 700;

// Line Heights
$line-height-tight: 1.25;
$line-height-base: 1.5;
$line-height-relaxed: 1.75;

// Letter Spacing
$letter-spacing-tight: -0.025em;
$letter-spacing-normal: 0;
$letter-spacing-wide: 0.025em;

// Heading Styles
@mixin heading-1 {
  font-size: $font-size-5xl;
  font-weight: $font-weight-light;
  line-height: $line-height-tight;
  letter-spacing: $letter-spacing-tight;
}

@mixin heading-2 {
  font-size: $font-size-4xl;
  font-weight: $font-weight-light;
  line-height: $line-height-tight;
}

@mixin heading-3 {
  font-size: $font-size-3xl;
  font-weight: $font-weight-regular;
  line-height: $line-height-tight;
}

@mixin heading-4 {
  font-size: $font-size-2xl;
  font-weight: $font-weight-regular;
  line-height: $line-height-base;
}

@mixin heading-5 {
  font-size: $font-size-xl;
  font-weight: $font-weight-medium;
  line-height: $line-height-base;
}

@mixin heading-6 {
  font-size: $font-size-lg;
  font-weight: $font-weight-medium;
  line-height: $line-height-base;
}

@mixin body-1 {
  font-size: $font-size-base;
  font-weight: $font-weight-regular;
  line-height: $line-height-base;
}

@mixin body-2 {
  font-size: $font-size-sm;
  font-weight: $font-weight-regular;
  line-height: $line-height-base;
}

@mixin caption {
  font-size: $font-size-xs;
  font-weight: $font-weight-regular;
  line-height: $line-height-base;
  letter-spacing: $letter-spacing-wide;
}

@mixin button {
  font-size: $font-size-sm;
  font-weight: $font-weight-medium;
  line-height: $line-height-base;
  letter-spacing: $letter-spacing-wide;
  text-transform: uppercase;
}
```

#### _tokens.scss
```scss
// Design Tokens
// Generated by /project-setup
// DO NOT EDIT MANUALLY - Regenerate with /project-setup

@import 'colors';
@import 'typography';

// Spacing (8pt Grid)
$spacing-0: 0;
$spacing-1: 0.25rem;   // 4px
$spacing-2: 0.5rem;    // 8px
$spacing-3: 0.75rem;   // 12px
$spacing-4: 1rem;      // 16px
$spacing-5: 1.25rem;   // 20px
$spacing-6: 1.5rem;    // 24px
$spacing-8: 2rem;      // 32px
$spacing-10: 2.5rem;   // 40px
$spacing-12: 3rem;     // 48px
$spacing-16: 4rem;     // 64px

// Border Radius
$radius-none: 0;
$radius-sm: 0.125rem;  // 2px
$radius-md: 0.25rem;   // 4px
$radius-lg: 0.5rem;    // 8px
$radius-xl: 1rem;      // 16px
$radius-full: 9999px;

// Shadows
$shadow-none: none;
$shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
$shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
$shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
$shadow-xl: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);

// Material Design Elevation
$elevation-1: 0 2px 1px -1px rgba(0,0,0,.2), 0 1px 1px 0 rgba(0,0,0,.14), 0 1px 3px 0 rgba(0,0,0,.12);
$elevation-2: 0 3px 1px -2px rgba(0,0,0,.2), 0 2px 2px 0 rgba(0,0,0,.14), 0 1px 5px 0 rgba(0,0,0,.12);
$elevation-4: 0 2px 4px -1px rgba(0,0,0,.2), 0 4px 5px 0 rgba(0,0,0,.14), 0 1px 10px 0 rgba(0,0,0,.12);
$elevation-8: 0 5px 5px -3px rgba(0,0,0,.2), 0 8px 10px 1px rgba(0,0,0,.14), 0 3px 14px 2px rgba(0,0,0,.12);

// Breakpoints
$breakpoint-xs: 0;
$breakpoint-sm: 600px;
$breakpoint-md: 960px;
$breakpoint-lg: 1280px;
$breakpoint-xl: 1920px;

// Z-Index Scale
$z-index-dropdown: 1000;
$z-index-sticky: 1020;
$z-index-fixed: 1030;
$z-index-modal-backdrop: 1040;
$z-index-modal: 1050;
$z-index-popover: 1060;
$z-index-tooltip: 1070;

// Transitions
$transition-fast: 150ms ease-in-out;
$transition-base: 250ms ease-in-out;
$transition-slow: 350ms ease-in-out;

// Animation Durations
$duration-fast: 150ms;
$duration-base: 250ms;
$duration-slow: 350ms;

// Component Tokens
$card-padding: $spacing-4;
$card-radius: $radius-md;
$card-shadow: $elevation-1;

$button-padding-x: $spacing-4;
$button-padding-y: $spacing-2;
$button-radius: $radius-md;

$input-padding-x: $spacing-4;
$input-padding-y: $spacing-2;
$input-radius: $radius-md;
$input-border-width: 1px;

$table-cell-padding: $spacing-3 $spacing-4;
$table-header-bg: $gray-50;

$toolbar-height: 64px;
$sidenav-width: 256px;
```

### Step 5: Update styles.scss

Ensure `frontend/src/styles.scss` imports the tokens:

```scss
// Import design tokens first
@import 'styles/tokens';

// Then your global styles
// ...
```

### Step 6: Verify Setup

```bash
# Check files were created
ls -la frontend/src/styles/

# Build to verify no errors
cd frontend && npm run build
```

## Output

```
PROJECT SETUP COMPLETE

Theme: Tech Innovation
Primary Color: #1976d2

Files Created:
- [X] frontend/src/styles/_colors.scss
- [X] frontend/src/styles/_typography.scss
- [X] frontend/src/styles/_tokens.scss

Design System Ready:
- Color palette with 10 shades
- Typography scale (12px - 48px)
- 8pt spacing grid
- Material Design elevations
- Responsive breakpoints

Next Steps:
1. Run /full-stack-feature to implement features
2. Use design tokens in all components
3. Reference _tokens.scss in wireframes
```

## When to Run

- **ONCE** at project start
- When changing the project theme
- When updating design system tokens

## Prerequisites

- Frontend project initialized (`npm install` complete)
- Theme Factory skill available
- UI Design System skill available
